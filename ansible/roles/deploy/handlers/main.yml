---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart robot services
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: restarted
  loop: "{{ robot_services }}"
